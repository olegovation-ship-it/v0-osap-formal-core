# Security and Integrity

Report parser crashes, path traversal, schema-reference confusion, non-deterministic replay, manifest mismatch, or CI bypass through a private security report in GitHub.

The checker treats input JSON as untrusted. It does not execute content from registry, evidence, proof, or fixture objects.
